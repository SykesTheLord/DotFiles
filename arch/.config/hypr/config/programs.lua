-- Shared program/mod constants used by other config modules (esp. keybindings).

return {
  terminal    = "alacritty",
  fileManager = "dolphin",
  menu        = "wofi --show drun",
  browser     = "zen-browser",
  mainMod     = "SUPER",
}
