# Suspend-then-hibernate on lid close
bindl = ,switch:off:Lid Switch, exec, systemctl suspend-then-hibernate

# Restore display (DPMS on) on lid open
bindl = ,switch:on:Lid Switch,  exec, hyprctl dispatch dpms on
